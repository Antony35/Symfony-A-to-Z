{% extends 'base.html.twig' %}

{% block title %}Réalisations{% endblock %}

{% block body %}
    <section>
        <h1>{{ blogPost.titre }}</h1>
        <p>{{ blogPost.contenu }}</p>
        <p>{{ blogPost.createdAt | date('d/m/Y') }}</p>
    </section>
{% endblock %}