{% block body %}
<section class="news">
    {% for actuality in actualities %}
        <a href="{{ path('detail_actuality', { 'slug' : actuality.slug } ) }}">
            <div>
                <h2>{{ actuality.titre }}</h2>
                <p>{{ actuality.contenu|u.truncate(150, '...', false) }}</p>
                <p>Crée le : {{ actuality.createdAt|date('d/m/Y') }}</p>
            </div>
        </a>
    {% endfor %}
</section>
{% endblock %}